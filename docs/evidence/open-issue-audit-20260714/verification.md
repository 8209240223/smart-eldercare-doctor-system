# 未关闭 Issue 审查与修复验证

## 修复范围

- #79：随访记录详情展示服药依从性。
- #99：收紧设备序列号并发绑定保护，不删除历史记录。
- #105：兼容旧“处理中”预警日志，避免显示为“已读”。
- #111：修正生活能力规则方向，并在报告层清洗非法残疾状态。
- #114、#115、#116、#117：补齐随访计划、任务和干预记录的医生归属、日期及关联一致性校验。

## 自动化验证

- 后端 Maven：180 项测试全部通过，0 失败，0 错误，0 跳过。
- 前端 lint：通过，仅保留 2 条既有 Fast Refresh 警告。
- 前端生产构建：通过。
- Chromium 本地回归：13 项检查全部通过，浏览器控制台和请求错误均为空。
- 普通问答：直接回答，不显示 Agent 工具步骤。
- 站内 Agent：真实调用站内工具，返回本地数据库老人总数 11。
- 单会话登录：旧会话失效，新会话可用。
- 语音功能：页面中不存在语音或麦克风控制。

## 数据保护

- 未执行数据库清空、老人档案删除或测试数据重置。
- 报告层清洗使用对象副本，不回写历史档案。
- 设备绑定修复保留现有历史记录，不增加可能因旧数据导致启动失败的唯一约束。

## 截图

- `assistant-ordinary-answer.png`：普通问答没有 Agent 执行步骤。
- `assistant-agent-query.png`：站内数据问题进入 Agent，并显示真实工具调用和结果。
- `followup-medication-compliance.png`：随访记录详情显示“服药依从性：规律”。
